import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginBottom: 10,
    borderBottomWidth: 1
  },

  comment__text: {
    textAlign: 'justify'
  }

})

export default styles;
